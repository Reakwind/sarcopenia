# .rscignore - Exclude files from shinyapps.io deployment
#
# For golem apps, exclude package source code (R/, man/) from deployment.
# The package will be built and installed from DESCRIPTION instead.
# This prevents the "sourcing R/ in a package" warning and conflicts.

# Exclude package source - will be built and installed via DESCRIPTION
^R$
^man$

# Exclude development files
^dev$
^tests$
^\.Rproj\.user$
^.*\.Rproj$

# Exclude renv - dependencies installed from DESCRIPTION instead
^renv$
^renv\.lock$

# Exclude git and version control
^\.git$
^\.github$

# Exclude other development files
^data-raw$
dev_history\.R$
^run_dev\.R$
^\.here$
^\.Rbuildignore$
^\.gitignore$

# Keep these for deployment:
# - app.R (entry point)
# - DESCRIPTION (package definition)
# - NAMESPACE (package exports)
# - inst/ (app resources: extdata, i18n, www)
